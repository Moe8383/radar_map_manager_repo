# Radar Map Manager (RMM)

![Logo](logo.png)

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-41BDF5.svg)](https://github.com/hacs/integration)

> 🇺🇸 **English User?** [Click here for English Documentation](README.md)

**Radar Map Manager (RMM)** 是专为 Home Assistant 打造的高级毫米波雷达可视化与数据融合集成。

它不仅仅是一个地图卡片，更是一个强大的**空间感知引擎**。RMM 能将家中分散的多个毫米波雷达（如 LD2410, LD2450, LD2460）的数据统一映射到您的户型图上，实现全屋人员定位追踪、轨迹可视化以及基于精确坐标的自动化触发。

> 🚀 **V1.0.0 正式发布！** 支持顶装模式 (Ceiling Mount)、排除干扰区 (Exclude Zones) 以及更丝滑的编辑器体验。

---

## ✨ 核心功能

### 1. 🎯 所见即所得的可视化编辑器
抛弃繁琐的 YAML 坐标计算！RMM 提供了一个交互式的前端编辑器：
* **拖拽配置**：直接在户型图上拖拽雷达位置。
* **自由变换**：支持雷达的旋转、缩放和镜像翻转。
* **双模式支持**：完美支持 **侧装 (Side Mount)** 和 **顶装 (Ceiling Mount)** 雷达，顶装模式下自动切换为圆形视场。

### 2. 🌐 多雷达数据融合 (Sensor Fusion)
RMM 的后端引擎能将多个雷达的目标点统一到一个坐标系中：
* **自动聚类**：当多个雷达探测到同一个人时，自动合并为一个目标，避免“影分身”。
* **盲区互补**：通过多雷达叠加，消除房间内的探测死角。

### 3. 🛡️ 强大的区域管理
* **监测区 (Monitor Zones)**：在地图上画个框，只有进入该区域才会触发。
* **排除区 (Exclude Zones)**：**解决误报的神器！** 在地图上圈出风扇、窗帘或绿植的位置，将其设为“排除区”，引擎会自动过滤掉该区域内的所有干扰信号。
* **自动化实体**：每个区域都会自动生成 `binary_sensor`，能够实现“人坐在沙发上打开电视”、“人走进厨房开启灯光”等精确自动化。

### 4. 📐 3D 空间修正
针对侧装雷达，RMM 内置了 3D 几何修正算法。根据雷达安装高度和目标高度，自动将斜距 (Slant Range) 还原为地距 (Ground Distance)，大幅提高定位精度。

---

## 🛠️ 支持的硬件

RMM 旨在兼容所有能够接入 Home Assistant 的毫米波雷达，只要它们能提供 X/Y 坐标或距离数据。

* **完美支持**：
    * **Hi-Link LD2450 / LD2460** (推荐，支持多目标坐标输出)
    * **Hi-Link LD2410** (支持单目标及 1D 距离模式)
* **连接方式**：
    * ESPHome (推荐)
    * MQTT
    * Zigbee (需支持坐标上报)

---

## 📦 安装方法

### 方式一：HACS 自动安装 (推荐)
1.  打开 HACS -> Integrations。
2.  点击右上角菜单 -> **Custom repositories**。
3.  填入本仓库地址 `https://github.com/Moe8383/radar_map_manager_repo`，类别选择 **Integration**。
4.  搜索 "Radar Map Manager" 并点击安装。
5.  重启 Home Assistant。

### 方式二：手动安装
1.  下载本仓库的 `custom_components/radar_map_manager` 文件夹。
2.  将其复制到您 HA 配置目录下的 `custom_components/` 中。
3.  重启 Home Assistant。

---

## ⚙️ 配置指南

### 第一步：添加集成
1.  前往 **配置** -> **设备与服务** -> **添加集成**。
2.  搜索 **Radar Map Manager** 并添加。

### 第二步：添加卡片
1.  在仪表盘点击“编辑仪表盘” -> “添加卡片”。
2.  搜索 **Radar Map Manager** 卡片。
3.  或者使用以下 YAML 配置：

```yaml
type: custom:radar-map-card
map_group: default
bg_image: /local/floorplan.png  # 您的户型图路径
target_radius: 8                # 目标点大小
target_colors:                  # 自定义目标颜色
  - "#00FF00"
  - "#00FFFF"
  - "#FF00FF"
